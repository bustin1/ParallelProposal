

#ifndef _DEBUG_H
#define _DEBUG_H

// DEBUG=0 -> normal mode
// DEBUG=1 -> rays shoot out of every particle
// DEBUG=2 -> rays shoot out of best particle
// BTW: When debug mode is one, the resampling occurs and THEN
// the redraw, so the rays should look wanked for a moment
#define DEBUG 0

#endif
